/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "ftv8eyq0it.ufs.sh",
      "flowrpowr1028.blob.core.windows.net",
    ],
  },
};

export default nextConfig;
